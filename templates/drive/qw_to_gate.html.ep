<div class="qw_define">
	<ol class="subTabs">
% foreach my $mod ( @$to_gate ) {
%	if ( $mod->{'fail'} ) {
		<li class="subTab fail" title="<%= $mod->{'fail'}%>"><%= $mod->{'fail'}%></li>
%	} else {
		<li class="subTab" title="<%= $mod->{'descr'}%>"><%= $mod->{'title'}%></li>
%	}
% }
	</ol>
	<div class=subHolder>
	</div>
</div>
