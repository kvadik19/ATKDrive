# Use encoding UTF-8
# Reload engine after change!
our_host	{http://atk.onpoz.ru}
our_site	{АЛВЕ ТрансКарго * транспортная компания}
our_email	{logist@alvetk.ru}
share_img	{}
html_dir	{/templates/html}		# HTML Templates
mail_dir	{/templates/mail}		# Service email templates
temp_dir	{/tmp}
conf_dir	{/config}
bkup_dir	{/backups}
img_dir		{/htdocs/img}
media_dir	{/media}
user_dir	{/media/user}
js_dir		{/htdocs/js}
url_prefix	{/htdocs}			# Remove path from *_dir to obtain url
encoding	{utf-8}				# HTML pages head value
db_encoding	{utf8}
db_base		{atk}
db_host		{}						# Nothing for localhost:default_port
db_user		{vdk:14rucoO}			# username:[password]
log_dir		{/log}
loglevel	{3}
logsize		{2048}
logcycle	{3}
reg_timeout	{12}				# Registration confirmation link timeout (hours)
inet_timeout	{5}				# Messaging over inet parameters without html-wrapping
inet_proto		{tcp}
inet_buffer		{2048}
smtp_host	{}				# To send mail via smtp. /usr/sbin/sendmail will be used if omit
smtp_login	{}				# smtp authorization as username:password. Ignore if free smtp
call_htpasswd	{/usr/bin/htpasswd}

